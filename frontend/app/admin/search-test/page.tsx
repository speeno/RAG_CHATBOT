import { SearchTestView } from "@/components/admin/SearchTestView";

export const metadata = { title: "검색 테스트 — AI 상담 도우미" };

export default function SearchTestPage() {
  return <SearchTestView />;
}
